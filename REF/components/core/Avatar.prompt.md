Initials avatar with a deterministic color derived from the name. Used in the sidebar user block and on assignees.

```jsx
<Avatar name="Nicolas Savard" />
<Avatar name="Sophie-Rose Dion" size="sm" />
```

Sizes `sm` 24 · `md` 32 · `lg` 40, or an explicit px number. Override the auto color with `color`.
